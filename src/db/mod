use sql::{PgPool, postgress::PgPoolOptions};

pub async fn connect_db(Database_url: &str) -> PgPool {
    PgPoolOptions::new()
        .max_connections(5)
        .connect(database_url)
        .await 
        .expect("Failed to connect to DB")
}